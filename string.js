const fname = "tester";
const role = " testing application";
const companyName = " abc";
const salPay = 12;
console.log(
  `im the working as ${fname} and my role is ${role} and my pay is ${salPay} i work in ${companyName}`
);
